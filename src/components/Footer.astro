---
import { getSiteSettings } from '../lib/sanity';

const settings = await getSiteSettings();

const currentYear = new Date().getFullYear();
const email = settings?.email || 'hello@dogdayssydney.com';
const phone = settings?.phone || '';
const siteName = settings?.siteName || 'Dog Days Sydney';
const tagline = settings?.tagline || "Your pup's home away from home";
const acknowledgement = settings?.acknowledgementOfCountry || '';
const abn = settings?.abn || '';
---

<footer class="footer">
  <div class="container footer-content">
    {acknowledgement && (
      <div class="acknowledgement">
        <p>{acknowledgement}</p>
      </div>
    )}

    <div class="footer-main">
      <div class="footer-brand">
        <a href="/" class="logo">
          <span class="logo-icon">🐕</span>
          <span class="logo-text">{siteName}</span>
        </a>
        <p class="tagline">{tagline}</p>
      </div>

      <div class="footer-links">
        <div class="link-group">
          <h4 class="link-group-title">Quick Links</h4>
          <a href="/gallery" class="footer-link">Gallery</a>
          <a href="/pricing" class="footer-link">Pricing & Terms</a>
          <a href="/bookings" class="footer-link">Booking Enquiry</a>
          <a href="/contact" class="footer-link">Contact</a>
        </div>

        <div class="link-group">
          <h4 class="link-group-title">Get in Touch</h4>
          <a href={`mailto:${email}`} class="footer-link">
            📧 {email}
          </a>
          {phone && (
            <a href={`tel:${phone.replace(/\D/g, '')}`} class="footer-link">
              📞 {phone}
            </a>
          )}
        </div>
      </div>
    </div>

    <div class="footer-bottom">
      <div class="copyright">
        <p>© {currentYear} {siteName}. Made with 🐾 and lots of love.</p>
        {abn && <p class="abn">ABN: {abn}</p>}
      </div>
    </div>
  </div>

  <div class="paw-prints" aria-hidden="true">
    <span class="paw">🐾</span>
    <span class="paw">🐾</span>
    <span class="paw">🐾</span>
  </div>
</footer>

<style>
  .footer {
    position: relative;
    background-color: var(--color-neutral);
    color: var(--color-secondary);
    padding: var(--space-3xl) 0 var(--space-xl);
    overflow: hidden;
  }

  .footer-content {
    position: relative;
    z-index: 1;
  }

  .acknowledgement {
    padding-bottom: var(--space-xl);
    margin-bottom: var(--space-xl);
    border-bottom: 1px solid rgba(255, 255, 255, 0.15);
  }

  .acknowledgement p {
    font-size: var(--text-sm);
    color: var(--color-secondary);
    opacity: 0.85;
    line-height: 1.7;
    font-style: italic;
    text-align: center;
    max-width: 700px;
    margin: 0 auto;
  }

  .footer-main {
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: var(--space-3xl);
    padding-bottom: var(--space-2xl);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .footer-brand {
    max-width: 300px;
  }

  .logo {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    font-family: var(--font-display);
    font-size: var(--text-xl);
    font-weight: 600;
    color: var(--color-secondary);
    margin-bottom: var(--space-md);
  }

  .logo-icon {
    font-size: 1.5em;
  }

  .logo-text {
    color: var(--color-secondary);
  }

  .tagline {
    font-size: var(--text-sm);
    color: var(--color-secondary);
    opacity: 0.75;
    line-height: 1.7;
  }

  .footer-links {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-2xl);
  }

  .link-group {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .link-group-title {
    font-family: var(--font-display);
    font-size: var(--text-lg);
    font-weight: 600;
    color: var(--color-secondary);
    margin-bottom: var(--space-sm);
  }

  .footer-link {
    color: var(--color-secondary);
    opacity: 0.75;
    font-size: var(--text-sm);
    transition: color var(--transition-fast), opacity var(--transition-fast);
  }

  .footer-link:hover {
    color: var(--color-accent);
    opacity: 1;
  }

  .footer-bottom {
    padding-top: var(--space-xl);
    text-align: center;
  }

  .copyright p {
    font-size: var(--text-sm);
    color: var(--color-secondary);
    opacity: 0.7;
  }

  .abn {
    margin-top: var(--space-xs);
    font-size: var(--text-xs);
    color: var(--color-secondary);
    opacity: 0.6;
  }

  .paw-prints {
    position: absolute;
    bottom: 0;
    right: 0;
    display: flex;
    gap: var(--space-xl);
    opacity: 0.1;
    font-size: 4rem;
    transform: rotate(-15deg);
  }

  .paw {
    animation: float 3s ease-in-out infinite;
  }

  .paw:nth-child(2) {
    animation-delay: 0.5s;
  }

  .paw:nth-child(3) {
    animation-delay: 1s;
  }

  @keyframes float {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-10px);
    }
  }

  @media (max-width: 768px) {
    .footer-content {
      display: flex;
      flex-direction: column;
      gap: var(--space-xl);
    }

    /* Reorder sections on mobile: Get in Touch first */
    .footer-main {
      order: 1;
      grid-template-columns: 1fr;
      gap: var(--space-2xl);
      padding-bottom: var(--space-xl);
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }

    .acknowledgement {
      order: 2;
      padding-bottom: var(--space-xl);
      margin-bottom: 0;
      border-bottom: none;
    }

    .footer-bottom {
      order: 3;
      padding-top: var(--space-md);
    }

    .footer-brand {
      max-width: none;
      text-align: center;
      order: 2;
    }

    .logo {
      justify-content: center;
    }

    .footer-links {
      grid-template-columns: 1fr;
      text-align: center;
      gap: var(--space-xl);
    }

    /* Hide Quick Links on mobile - already in header nav */
    .link-group:first-child {
      display: none;
    }

    /* Get in Touch appears first within footer-links */
    .link-group:last-child {
      order: -1;
    }
  }
</style>
